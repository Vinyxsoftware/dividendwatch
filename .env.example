# PostgreSQL connection string (required)
DATABASE_URL="postgresql://user:password@localhost:5432/dividendwatch"

# Secret used to authenticate POST /api/refresh (required)
# Generate with: openssl rand -hex 32
REFRESH_SECRET="your-secret-here"
